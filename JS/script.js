// This function stores the answer of the test questions
function submitButton(){
    var score = 0;
    var correctAnswer = document.first.question1.value;
    if (correctAnswer == "B")(score +=10)
    document.write(score);
}